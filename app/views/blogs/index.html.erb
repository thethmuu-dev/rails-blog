<h1>Blog list</h1>
<table class="table">
  <tr>
    <th>Title</th>
    <th>Content</th>
    <th colspan=2>&#9899;</th>
  </tr>
  <% @blogs.each do |blog| %>
  <tr>
    <td>
      <%= blog.title %>
    </td>
    <td>
      <%= blog.content %>
    </td>
    <td>
      <%= link_to raw("&#10145;"), blog_path(blog.id) %>
    </td>
    <td>
      <%= link_to  raw("&#9935;"), edit_blog_path(blog.id) %>
    </td>
  </tr>
  <% end %>
</table><br><br>

<p class="shadow"><%= link_to  "Go to Blog Create Page", new_blog_path %></p>